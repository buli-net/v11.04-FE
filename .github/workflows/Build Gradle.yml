name: Build Bitcoin Wallet v11 - Gradle Build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Print runner info
        run: |
          echo "=== RUNNER INFO ==="
          uname -a
          date -u

      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
          clean: true

      - name: Verify commit details
        run: |
          git log -1 --pretty=full
          git rev-parse HEAD > commit.txt

      - name: Setup Gradle (cache + UI)
        uses: gradle/actions/setup-gradle@v5
        with:
          cache-cleanup: never

      # Java 11 cho Gradle 6.9.4
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '11'

      - name: Verify Java
        run: java -version

      - name: Install Android SDK 34
        run: |
          export ANDROID_HOME=$HOME/android-sdk
          mkdir -p $ANDROID_HOME/cmdline-tools
          cd /tmp
          wget -q https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip
          unzip -q commandlinetools-linux-11076708_latest.zip -d $ANDROID_HOME/cmdline-tools
          mv $ANDROID_HOME/cmdline-tools/cmdline-tools $ANDROID_HOME/cmdline-tools/latest
          export PATH=$ANDROID_HOME/cmdline-tools/latest/bin:$PATH
          yes | sdkmanager --sdk_root=$ANDROID_HOME --licenses
          sdkmanager --sdk_root=$ANDROID_HOME "platform-tools" "platforms;android-34" "build-tools;34.0.0"
          echo "ANDROID_HOME=$ANDROID_HOME" >> $GITHUB_ENV

      - name: Create local.properties
        run: echo "sdk.dir=$ANDROID_HOME" > local.properties

      # FIX: Gradle 6.9.4 (vừa >=5.6 cho protobuf, vừa <7.0 cho project cũ)
      - name: Setup Gradle wrapper
        run: |
          if [ ! -f ./gradlew ]; then
            wget -q https://services.gradle.org/distributions/gradle-6.9.4-bin.zip
            unzip -q gradle-6.9.4-bin.zip
            ./gradle-6.9.4/bin/gradle wrapper --gradle-version 6.9.4
          fi
          chmod +x gradlew
          ./gradlew --version

      - name: Clean project
        run: ./gradlew clean

      - name: Build release APKs
        run: |
          unset ANDROID_NDK_HOME
          ./gradlew :wallet:assembleRelease -x lint --stacktrace

      - name: Collect APKs
        run: |
          mkdir -p out
          find wallet/build/outputs/apk -name "*release*.apk" -exec cp {} out/ \;
          sha256sum out/*.apk > out/raw-sha256.txt

      - name: Find apksigner
        run: |
          APK=$(find "$ANDROID_HOME/build-tools" -name apksigner | sort -V | tail -1)
          echo "APKSIGNER=$APK" >> $GITHUB_ENV

      - name: Decode keystore
        run: |
          echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 -d > keystore.jks
          keytool -list -v -keystore keystore.jks -storepass "${{ secrets.KEYSTORE_PASSWORD }}" -alias "${{ secrets.KEY_ALIAS }}" | tee keystore-info.txt

      - name: Sign APKs
        run: |
          mkdir -p signed
          for apk in out/*.apk; do
            base=$(basename "$apk" .apk | sed 's/-unsigned//')
            "$APKSIGNER" sign --ks keystore.jks --ks-pass pass:"${{ secrets.KEYSTORE_PASSWORD }}" --ks-key-alias "${{ secrets.KEY_ALIAS }}" --key-pass pass:"${{ secrets.KEY_PASSWORD }}" --out "signed/${base}-signed.apk" "$apk"
          done

      - name: Verify signatures
        run: |
          for apk in signed/*.apk; do
            "$APKSIGNER" verify --verbose --print-certs "$apk" | tee verify-$(basename $apk).txt
          done

      - name: Generate report
        run: |
          {
            echo "Build Date: $(date -u)"
            echo "Java: 11"
            echo "Gradle: 6.9.4"
            echo "Commit: $(cat commit.txt)"
            sha256sum signed/*.apk
          } > report.txt

      - name: Upload artifacts
        uses: actions/upload-artifact@v6.0.0
        with:
          name: apk-v11-signed-gradle
          path: |
            signed/*.apk
            report.txt
            keystore-info.txt
            out/raw-sha256.txt
            verify-*.txt
